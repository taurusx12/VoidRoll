# VoidRoll Reborn — Phase 21 Variant Fix

## Run

```bash
cd ~/project/src
node scripts/voidroll-create-variants-fixed.js
cat reports/characters_VARIANTS_CREATED_FIXED.json
```

This fixes the broken `id: ,` issue and uses anime targeting so Madara maps to Naruto and Ichigo maps to Bleach.
