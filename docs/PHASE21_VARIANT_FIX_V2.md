# Phase 21 Variant Fix V2

This version only writes fields that exist in your current Character Prisma model.

Run:

```bash
cd ~/project/src
node scripts/voidroll-create-variants-fixed.js
cat reports/characters_VARIANTS_CREATED_FIXED.json
```
