// UNLIMITED INVENTORY PATCH

// Replace any inventory limit with:

const INVENTORY_LIMIT = 999999999;

// Replace:
if (inventoryCount >= 200)

// With:
if (inventoryCount >= 999999999)

// Replace:
if (cards.length >= 200)

// With:
if (cards.length >= 999999999)

// Or delete the limit checks completely.
